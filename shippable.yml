language: python

python:
  - 3.6

install:
  - pip install -r requirements.txt
  - https://github.com/secdev/scapy
  - cd scapy
  - sudo python setup.py install
  - cd ../

script:
  - python run.py 44
